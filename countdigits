#include<stdio.h>
#include<stdlib.h>

int count(int num)
{ int c=0;
  while(num!=0)
  {
    num=num/10;
    ++c;
  }
  return c;
}
int main()
{
    int num;
    printf("Enter the number");
    scanf("%d",&num);
    printf("No. of digits in number=%d",count(num));
}
